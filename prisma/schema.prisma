generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id       String  @id @default(uuid())
  username String  @unique
  password String
  todos    Todos[]
}

model Todos {
  id          String    @id @default(uuid())
  title       String
  description String
  isCompleted Boolean   @default(false)
  createdAt   DateTime  @default(now())
  dueDate     DateTime?
  user        User?     @relation(fields: [userId], references: [id], onDelete: Cascade)  
  userId      String?
}

//onDelete:Cascade  use krne se user delete krne se uska todos/posts bhi usi k saath saath delete ho jata hai
// iske liye mujhe fir se migrate krna pdta hai
// if don't  want to use this another way to delete user with thier posts/todos is
//$awit prisma.$transaction([deleteTodos,deleteUser]) order matter krta hai and 
// in this case await nhi use kiya jaata hai 
